#include <stdio.h>

int main(void) {
	// i 는 변수 
	// p는 i를 가리키는 포인터 

	// (a) *p -> i
	// (b) &p -> p의 주소값
	// (c) *&p -> p
	// (d) &*p -> p
	// (e) *i -> 
	// (f) &i -> i의 주솟값
	// (g) *&i -> i 
	// (h) &*i ->

	// (a), (g) 

	return 0;
}