#include <stdio.h>

int main(void) {

	// (i += 1) 은 출력하면 바로 i의 값이 1이 증가되어 나온다. 
	// ++i도 출력하면 바로 i의 값이 1 증가되어 나오는 반면
	// i++은 출력했을 때 i의 값이 1 증가되기 이전 값이 나온다. 
	//따라서 (i += 1) 과 같은 것은 ++i이다. 

	return 0;
}