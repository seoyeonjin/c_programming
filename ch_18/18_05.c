// (a) false - block안에 선언되면 block scope을 갖지만 static duration을 갖는다. 
// (b) false - function 안에 선언된 variable도 extern으로 선언될 수 있다. 
// (c) true - internal linakage인 변수는 static storage duration을 가진다. 
// (d) true - paramter로 정의된 변수는 함수 안에서만 사용할 수 있는 변수