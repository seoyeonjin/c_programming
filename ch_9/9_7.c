#include <stdio.h>

int f(int a, int b);

int main(void) {

	// (a) 가장 적절하다. 
	// (b) x가 double이므로 x에 결과값이 할당될 수 있다. 
	// (c) a와 b는 int로 들어간다. 
	// (d) a와 b는 int로 들어가고 결과값은 double로 저장된다. 
	// (e) return값은 무시된다. 

	return 0;
}