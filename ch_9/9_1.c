#include <stdio.h>

double triangle_area(double base, double height)
{
	product = base * height;
	return product / 2;
}

double product;

int main(void) {

	// 2가지 에러 
	// 1) triangle_area 선언과 정의 사이에 다른 코드가 있다.
	// 2) 두번째 parameter의 type이 빠져있다. 
	return 0;
}